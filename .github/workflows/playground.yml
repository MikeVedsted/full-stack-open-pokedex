name: Playground

on:
  push:
    branches:
      - master
  pull_request:
    branches: [master]
    types: [opened, synchronize]

jobs:
  simple_deployment_pipeline:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - name: gihub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: commits
        env:
          COMMITS: ${{ toJson(github.event.commits) }}
        run: echo "$COMMITS"
      - name: commit messages
        env:
          COMMIT_MESSAGES: ${{ toJson(github.event.commits.*.message) }}
        run: echo "$COMMIT_MESSAGES"

      - name: array joined
        env:
          COMMIT_ARR: ${{ contains(toJson(join(github.event.commits.*.message)), '#skip') }}
        run: echo "$COMMIT_ARR"

      - name: this
        if: ${{ github.event_name == 'push' }} && !contains(toJson(join(github.event.commits.*.message)), '#skip')
        # if: "!contains(toJson(join(github.event.commits.*.message)), '#skip')"
        run: echo "works? :D!"

